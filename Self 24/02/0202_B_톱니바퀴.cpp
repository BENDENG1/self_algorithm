/*
톱니 바퀴

k번 회전 / 시계,반시계 둘다됨

n극은 0 s극은 1
회전 횟수 k

2번,6번인덱스 확인 및 회전은 맨뒤 빼서 앞으로 혹은 맨앞빼서 뒤로 하면 됨 
*/
#include <iostream>
#include <vector>
#include <string>

int main(){
    // ios_base::sync_with_stdio(0),cin.tie(0),cout.tie(0);
    string s;

    for(int i = 0 ; i < 4; i++){
        cin >> s;
    }
}
